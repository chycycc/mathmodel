# 问题一、问题二验证报告

## 结论

PASS（针对当前代码、当前导出结果和已声明的模型假设）。问题二是 ALNS 启发式方案，PASS 表示约束和代码—结果一致，不表示全局最优。

## 检查项

|检查项|结果|证据|
|---|---|---|
|问题一货箱覆盖|通过|80/80，唯一货箱编号|
|问题一质量、体积、SOC|通过|独立重算无错误|
|问题二货箱覆盖|通过|80/80，唯一货箱编号|
|问题二多点交付累计交接时间|通过|逐站累计公式重算误差 ≤0.1 s|
|问题二质量、体积、服务区归属|通过|独立重算无错误|
|实体无人机时间重叠|通过|0|
|共享电池时间重叠|通过|0|
|问题二硬时限|通过|0 箱违约|
|Python 语法|通过|`py_compile` 通过|

## 运行证据

正式 Q2 运行使用种子 42、43、44，每种 100 次迭代，摘要保存于 `Q2_ALNS_多随机种子摘要.csv`。最佳方案为 seed 42：24 架次、78.265387 kWh、7730.5 s。

## 复核脚本

`D:\数学建模2026\scratch\verify_q1_q2_current.py` 会加载当前结果并调用 `data_loader.py`、`problem1.py`、`problem2.py` 中的物理函数。脚本退出码为 0 表示通过。

## 仍需注意

问题二的 ALNS 结果依赖随机种子和迭代次数，报告应称为可行启发式方案，不宣称全局最优。问题三、问题四尚未接入本次验收。

