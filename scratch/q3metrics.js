const fs=require('fs');function csv(p){let z=fs.readFileSync(p,'utf8').replace(/^\uFEFF/,'').trim().split(/\r?\n/);let h=z[0].split(',');return z.slice(1).map(l=>{let a=l.split(','),o={};h.forEach((k,i)=>o[k]=a[i]||'');return o;});}let c=csv('D:/python project/mathmodel/results/Q3/Q3_通信保障.csv');let margins=c.map(x=>+x['直连阈值_dB']-+x['直连损耗_dB']);console.log(JSON.stringify({samples:c.length,minMargin:Math.min(...margins),maxLoss:Math.max(...c.map(x=>+x['直连损耗_dB'])),minLoss:Math.min(...c.map(x=>+x['直连损耗_dB'])),statuses:[...new Set(c.map(x=>x['通信状态']))]}));