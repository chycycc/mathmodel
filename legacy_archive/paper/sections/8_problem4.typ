= 问题四的模型建立与求解

== 航线强绑定约束与任务图谱聚类

=== 多点串联航线硬约束绑定
在问题二与问题三得到的全局联合最优调度方案中，架次 T002 串联访问了服务区 S002 与 S004。
根据赛题对任务分区独立性的基本准则：“每个服务区必须且只能属于一个任务组，每个任务组由相对独立的无人机系统执行”。若将 S002 与 S004 分离至不同任务组，将导致该串联飞行航段跨组割裂，违背既有航线完整性与自治要求。
因此，必须在拓扑图上将 S002 与 S004 进行**超节点强绑定收缩**（Contraction），形成不可拆分的刚性原子单元：
$
"S002", "S004" in cal(G)_k, quad exists! k in {1, dots, K}
$

=== 基于空间几何与运量的图谱聚类划分
构建以 15 个受灾服务区为顶点的无向加权图 $cal(G) = (cal(V), cal(E))$。任意两点 $S_i, S_j$ 之间的边权 $W_(i j)$ 综合衡量空间欧氏距离与货箱总量阻抗：
$
W_(i j) = cases(
  +infinity\, & (i, j) = ("S002", "S004"),
  exp(- (d^2(S_i, S_j)) / (2 sigma_d^2) - (|w_i - w_j|^2) / (2 sigma_w^2))\, & "其他"
)
$
分别针对 $K=2$ 与 $K=3$ 求解最小割或谱聚类（Spectral Clustering），得到兼顾空间紧凑度、组间物资均衡度与航线硬约束的最佳分区方案。

== 任务分区方案设计与物理网格定位

=== K=2 分区方案（东西两翼对半分区）
- *任务组 G1（西区与近中低谷单元）*：
  包含服务区：`S001, S003, S006, S007, S011, S015`。
  涵盖物资 40 箱，总重 389.0kg，总体积 1.042$m^3$。对应执飞 12 个运输架次。
- *任务组 G2（北部重载攻坚与东部深山单元）*：
  包含服务区：`S002, S004, S005, S008, S009, S010, S012, S013, S014`。
  涵盖物资 40 箱，总重 369.0kg，总体积 0.974$m^3$。对应执飞 15 个运输架次。
- *分区评价*：两组在物资总箱数上达到绝对对半平衡（40箱 vs 40箱，占比 50.0% vs 50.0%），总质量仅相差 20kg（389kg vs 369kg），且以中央山脊为天然分界线，调度边界极为清晰。

=== K=3 分区方案（三格专业化联防）
- *任务组 G1（西区陡峭山地单元）*：
  包含服务区：`S003, S006, S007, S015`。涵盖物资 22 箱，总重 210.0kg，执飞 8 个架次。
- *任务组 G2（中北部重载枢纽单元）*：
  包含服务区：`S001, S002, S004, S005, S008, S011`。涵盖物资 43 箱，总重 423.0kg，执飞 10 个架次。
- *任务组 G3（东部深山联防单元）*：
  包含服务区：`S009, S010, S012, S013, S014`。涵盖物资 15 箱，总重 125.0kg，执飞 9 个架次。

图 8-1(a) 展示了服务区空间拓扑分布与分区划分边界。

#figure(
  image("../../figures/fig5_task_partitioning_topology.pdf", width: 95%),
  caption: [服务区空间地理分区拓扑与集中统筹 vs 独立配置资源开销对比图],
) <fig_partition_topo>

== 独立资源配置核算与集中池化效应深度剖析

=== 区间并发扫描法核算原理
当各任务组被设定为完全独立的执行单元时，组与组之间禁止跨组借调无人机、电池或中继设备。
对每一任务组 $cal(G)_k$：
1. *运输无人机峰值需求*：
   设架次 $p in cal(G)_k$ 执飞机型为 $m$，机体占用时间区间为 $[t_("start")^p, t_("arr")^p]$。无人机需求数等于该区间在机型 $m$ 上的最大时域重叠数：
   $
   N_m^((k)) = max_t { sum_(p in cal(G)_k, m(p)=m) bb(I)(t_("start")^p <= t <= t_("arr")^p) }
   $
2. *共享电池组峰值需求*：
   电池在无人机降落后必须进入两阶段充电时钟，占用时间延展为 $[t_("start")^p, t_("arr")^p + Delta t_("charge")(b)]$。电池需求数为时域处于“飞行中 + 正在充电”的峰值总和：
   $
   B_m^((k)) = max_t { sum_(p in cal(G)_k, m(p)=m) bb(I)(t_("start")^p <= t <= t_("arr")^p + Delta t_("charge")(b)) }
   $
3. *中继机与能源组件峰值需求*：
   同理统计为该组运输架次提供中继保障的空中中继机及处于充电状态的能源组件并发峰值 $N_R^((k))$ 与 $B_("MOD")^((k))$。

=== 资源配置结果与库存缺口定量比对
表 8-1 汇总了 $K=2$ 与 $K=3$ 下各分区的独立资源配置需求，并与现有仓库可用库存基准进行了精准对账。

#align(center)[
  #table(
    columns: (1.5cm, 1.4cm, 1.1cm, 1.1cm, 1.1cm, 1.2cm, 1.2cm, 1.2cm, 1.1cm, 1.2cm),
    align: (center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon),
    stroke: none,
    table.hline(stroke: 1.2pt),
    table.header([*分区模式*], [*任务组*], [*A机*], [*B机*], [*C机*], [*A电池*], [*B电池*], [*C电池*], [*中继机*], [*组件*]),
    table.hline(stroke: 0.6pt),
    [*现有库存*], [基准], [4], [2], [2], [6], [4], [4], [2], [6],
    table.hline(stroke: 0.4pt),
    [K=2], [G1], [3], [2], [2], [4], [3], [2], [1], [2],
    [], [G2], [4], [2], [2], [5], [3], [3], [2], [4],
    [], [*合计需求*], [*7*], [*4*], [*4*], [*9*], [*6*], [*5*], [*3*], [*6*],
    [], [*库存缺口*], [*-3*], [*-2*], [*-2*], [*-3*], [*-2*], [*-1*], [*-1*], [*0*],
    table.hline(stroke: 0.4pt),
    [K=3], [G1], [3], [1], [1], [4], [2], [2], [1], [2],
    [], [G2], [3], [2], [2], [3], [3], [3], [2], [4],
    [], [G3], [2], [2], [0], [3], [3], [0], [2], [3],
    [], [*合计需求*], [*8*], [*5*], [*3*], [*10*], [*8*], [*5*], [*5*], [*9*],
    [], [*库存缺口*], [*-4*], [*-3*], [*-1*], [*-4*], [*-4*], [*-1*], [*-3*], [*-3*],
    table.hline(stroke: 1.2pt)
  )
]

=== 理论启示：集中池化效应（Pooling Effect）与分散配置开销
对比图 8-1(b) 的柱状分析，可以得出极具说服力的管理科学结论：
1. *集中调度的池化复用红利（Pooling Efficiency）*：
   在问题二和问题三的全局统筹下，总共仅需 8 架运输机（4 A, 2 B, 2 C）、14 组电池和 2 架中继机，即以 **零库存缺口** 100% 履约完成抢险救灾。原因在于集中调度实现了*时差复用与资产共享*——例如 U07 执行完 S001 任务充电就绪后，可继续执飞 S002 任务；MOD_01 在 RT01 返航后，空闲中继机可换装 MOD_03 执行后续架次；
2. *独立分区的分散开销机制（Decentralization Overhead）*：
   一旦推行物理隔离的独立分区，各组为了保证自身防区的首批时效性，在救援启动前 1 小时内均存在剧烈的运力脉冲，导致“并发峰值叠加”。
   - 在 $K=2$ 下，运输无人机总需求由 8 架增加至 15 架（缺口 7 架），动力电池总需求由 14 组增至 20 组（缺口 6 组），中继机需求 3 架（缺口 1 架），能源组件需求 6 组（刚好满足）；
   - 在 $K=3$ 下，由于 3 个独立组在时空上均存在脱离 G01 的深山航段，各组中继并发不可复用，使得中继机总需求猛增至 5 架（**缺口高达 3 架**），能源组件需求增至 9 组（缺口 3 组）；
3. *应急决策建议*：
   若灾区必须设立分区分队以压实责任，**强烈推荐采用 $K=2$ 东西两翼对半分区**。因其仅需补充 7 架运输无人机和 1 架中继机即可平稳运行，且能源组件完全无需额外采购；若资源条件极其紧缺，则应坚决维持统一指挥与资源池化调度中心，以最小化资产投入获得最高救援效能。