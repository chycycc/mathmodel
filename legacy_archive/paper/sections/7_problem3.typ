= 问题三的模型建立与求解

== 30m DEM 空间光线追踪与射频链路预算

=== 三维视距通视判别算法（Ray-Casting LOS）
无线电波在 2.4GHz 频段下主要沿直线传播。设发射节点通信天线端点三维坐标为 $A(x_1, y_1, z_1)$，接收节点为 $B(x_2, y_2, z_2)$。
三维空间直线段的参数化方程为：
$
bold(r)(alpha) = (1 - alpha) bold(r)_A + alpha bold(r)_B, quad alpha in [0, 1]
$
以空间步长 $Delta s = 20"m"$ 沿线段进行离散采样。对于每个采样点 $alpha_j$，其投影经纬度坐标为 $("lon"(alpha_j), "lat"(alpha_j))$，视线在当前水平位置的空间高度为：
$
z_("ray")(alpha_j) = (1 - alpha_j) z_1 + alpha_j z_2
$
通过对 $1309 times 1486$ 的 30m DEM 地形栅格进行双线性插值，查询该点地表的真实地面海拔 $H_("DEM")("lon"(alpha_j), "lat"(alpha_j))$。
若存在任意采样点满足：
$
z_("ray")(alpha_j) <= H_("DEM")(alpha_j)
$
则判定视线被山区地面山体遮挡，遮挡指示变量置为 $I_("obs")(A, B) = 1$；若全路径上视线均高于地表，则判定为视距通视，置 $I_("obs")(A, B) = 0$。

=== 双向无线射频链路预算与直连断开机理解析
根据自由空间传播损耗模型（FSPL），在载波频率 $f = 2400"MHz"$、三维直线欧式距离为 $d$（单位 km）时，基本路径损耗为：
$
L_("free")(d) = 32.44 + 20 lg(2400) + 20 lg(d) = 100.04 + 20 lg(d) quad ("dB")
$
考虑山体遮挡附加衰减 $L_("obs") = 10.0"dB"$，总路径损耗为：
$
L_("path")(A, B) = L_("free")(d) + I_("obs")(A, B) dot L_("obs")
$
根据通信收发双向能量守恒定理，通信方向 $1 -> 2$ 的最大允许损耗为：
$
L_("max")^(1 -> 2) = P_t(1) + G_t(1) + G_r(2) - P_("sens")(2) - M - L_("sys")
$
对于固定网关 G01（端点海拔 $127.7 + 20 = 147.7"m"$）与运输无人机之间的直连链路：
- G01 $->$ UAV：$27 + 12 + 3 - (-98) - 8 - 3 = 129.0"dB"$；
- UAV $->$ G01：$20 + 3 + 12 - (-98) - 8 - 3 = 122.0"dB"$；
- 双向门限取交集极小值：$L_("max")^("direct") = min(129.0, 122.0) = 122.0"dB"$。

在无山体遮挡的理想视距下：
$
100.04 + 20 lg(d) <= 122.00 => lg(d) <= 1.098 => d <= 12.53"km"
$
然而，一旦发生山体阻断（$I_("obs") = 1$），总损耗增加 10dB，等效自由空间门限降为 $112.0"dB"$：
$
100.04 + 20 lg(d) <= 112.00 => lg(d) <= 0.598 => d <= 3.96"km"
$
由此揭示了深山通信阻断的本质物理原因：
- S001（3.06km）、S006（3.18km）、S011（2.82km）与 O01 距离均小于 3.96km，即便低空阶段受到微弱地形遮挡，其总损耗仍处于 $119.0 ~ 120.1"dB" <= 122.0"dB"$，能够维持直连通信；
- 其余 12 个服务区（S002, S003, S004, S005, S007, S008, S009, S010, S012, S013, S014, S015）距离均在 $4.5 ~ 8.1"km"$ 以上，且中间横亘着海拔 $400 ~ 700"m"$ 的高耸山峰。视距必然被切断，其总损耗高达 $123.2 ~ 128.2"dB"$，远远突破了 122.0dB 临界门限，直连必定中断，必须依赖空中中继机实现高空信号跨越。

图 7-1 给出了基于上述物理机理构建的空间通视判别与中继决策流。

#figure(
  image("../../figures/fig_flow_q3.pdf", width: 75%),
  caption: [基于 30m DEM 射线追踪的视距通视判别与空地中继状态转移决策流程图],
) <fig_flow_q3>

== 中继无人机三维空间悬停优化与服务调度

=== 中继机双向链路门限与悬停阵位搜索
中继系统采用 R 型中继机与两幅专用定向/全向天线（回传天线与接入天线）：
1. *回传链路（Relay $<->$ G01）*：
   - G01 $->$ Relay：$27 + 12 + 8 - (-98) - 8 - 3 = 134.0"dB"$；
   - Relay $->$ G01：$19 + 8 + 12 - (-98) - 8 - 3 = 126.0"dB"$；
   - 回传门限：$L_("max")^("backhaul") = 126.0"dB"$（视距通视下最大覆盖距离达 19.86km）；
2. *接入链路（Relay $<->$ 运输无人机）*：
   - Relay $->$ UAV：$20 + 6 + 3 - (-98) - 8 - 3 = 116.0"dB"$；
   - UAV $->$ Relay：$20 + 3 + 6 - (-98) - 8 - 3 = 116.0"dB"$；
   - 接入门限：$L_("max")^("access") = 116.0"dB"$（视距通视下最大覆盖半径 6.28km）；
3. *离地净空约束*：悬停高程必须满足地面净空 $H_("hover") - H_("DEM")(x_r, y_r) <= 300"m"$。

通过在全区域以 500m 网格进行全空域通视投影遍历搜索，优化出两个战略悬停阵位：
- *西部主悬停阵位（West Station）*：经度 $109.20598°$，纬度 $23.04455°$，地面高程 $406.2"m"$，悬停海拔 **$686.2"m"$**（净空 280m）。
  - 与 G01 视线完全无遮挡，回传损耗仅 $113.6"dB"$（具有 12.4dB 巨大裕量）；
  - 高空俯瞰覆盖西部山谷与中北部全部服务区（S001~S005, S007~S009, S011, S015）；
- *东部辅悬停阵位（East Station）*：经度 $109.27737°$，纬度 $23.02474°$，地面高程 $410.5"m"$，悬停海拔 **$690.5"m"$**（净空 280m）。
  - 回传损耗 $124.2"dB" <= 126.0"dB"$；
  - 专职覆盖东部深山服务区（S010, S012, S013, S014）。

=== 中继架次排班与空地连续通信保障结果
配置 2 架实体中继机（R01, R02）与 6 组高能能源组件（MOD_01~MOD_06，额定容量 8.0kWh，可用容量 3.2kWh，悬停+通信总功率 1.10kW）。
受益于问题二运输调度的大幅提速（完工时间压缩至 7353.1s / 2.04h），全空域通信遮挡时隙由原 1.28 万秒大幅收窄至 1080s~6926s，中继出动任务得以由原先冗余的 6 架次精简为 **4 个高饱和度架次**，如表 7-1 所示。

#align(center)[
  #table(
    columns: (1.5cm, 1.4cm, 1.6cm, 1.8cm, 2.0cm, 2.0cm, 2.0cm, 1.7cm),
    align: (center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon, center + horizon),
    stroke: none,
    table.hline(stroke: 1.2pt),
    table.header([*中继架次*], [*机型*], [*组件*], [*开始时刻(s)*], [*建链完成(s)*], [*服务结束(s)*], [*返回时刻(s)*], [*返航SOC*]),
    table.hline(stroke: 0.6pt),
    [RT01], [R01], [MOD_01], [450.0], [1116.1], [4000.0], [4502.7], [64.3%],
    [RT02], [R02], [MOD_02], [380.0], [1070.1], [3700.0], [4227.0], [66.3%],
    [RT03], [R01], [MOD_03], [4850.0], [5516.1], [7200.0], [7702.7], [75.8%],
    [RT04], [R02], [MOD_04], [4550.0], [5240.1], [7000.0], [7527.0], [74.6%],
    table.hline(stroke: 1.2pt)
  )
]

图 7-2 直观展现了典型航线 O01 至 S008 的空间地形遮挡与高空中继射线追踪几何剖面，以及 28 个运输架次全时域 100% 连续通信保障时序。

#figure(
  image("../../figures/fig4_coverage_and_relay_profile.pdf", width: 95%),
  caption: [空间遮挡剖面机理与 28 个运输架次 100% 连续通信保障时序图],
) <fig_comm_profile>

统计表明：全周期 28 个运输架次共细分为 **88 个通信阶段**，其中固定网关直连阶段 **52 个**，空中中继机无缝接力保障阶段 **36 个**。全过程**通信盲区与中断时长恒为 0 秒（100.0% 连续覆盖）**，不仅实现了绝对无缝的指挥通信，而且能源组件仅消耗 MOD_01~MOD_04 共 4 组，为指挥中心节约出 MOD_05 与 MOD_06 两组战略战备冗余（节省率 33.3%）。各架次返航 SOC 均保持在 $64.3\% ~ 75.8\%$ 之间，两架中继机地面周转时间分别为 347.3s 与 323.0s（均严格大于法定 300 秒），完全符合航空器安全规程。