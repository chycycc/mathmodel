= 问题二的模型建立与求解

== 异构多机多架次时空协同网络模型

=== 状态变量与多点回路定义
在问题二中，允许多个服务区在单一架次内进行串联回路访问，且调度资源被严格约束为 8 架实体运输机 $cal(U) = {"U01", dots, "U08"}$（4 A, 2 B, 2 C）与 14 组共享电池 $cal(B) = {"BAT_A_01"} dots {"BAT_C_04"}$。
定义架次 $p$ 的访问路径节点序列为：
$
sigma_p = ("O01", v_1, v_2, dots, v_l, "O01"), quad v_j in {S_1, dots, S_(15)}
$
引入决策与状态变量：
- $u(p) in cal(U)$：执飞架次 $p$ 的实体无人机编号；
- $b(p) in cal(B)$：架次 $p$ 所挂载的共享电池编号（要求机型一致，即 $u(p)$ 与 $b(p)$ 属于同一机型类别 $m$）；
- $t_("start")^p$：架次 $p$ 开始进行地面装载与起飞准备的时刻；
- $t_("dep")^p$：无人机完成准备与装载正式起飞离地的时刻；
- $t_("arr")^p$：无人机完成沿途全部交付并返回 $"O01"$ 降落的时刻；
- $t_("deliv")^k$：货箱 $k$ 在其目标服务区完成卸载交付的时刻。

=== 动态剩余载荷递减能耗积分模型
当无人机沿航路序列连续配送时，每经过一个受灾点交付一部分物资，后续航段的有效载重随之减少。设架次 $p$ 起飞时的初始总载质量为 $w_(p, 0) = sum_(k in cal(K)_p) w_k$。
对于航路中的第 $j$ 个航段 $(v_(j-1) -> v_j)$，其承载的瞬时剩余载重为：
$
w_(p, j) = w_(p, 0) - sum_(s=1)^(j-1) sum_(k in cal(K)_p inter cal(K)_(v_s)) w_k
$
该航段的飞行时间 $Delta t_j$ 与消耗电能 $Delta E_j$ 由 `data_loader` 物理引擎根据实时载荷 $w_(p, j)$ 精确积分计算：
$
Delta E_j = (d(v_(j-1), v_j)) / (R(m, w_(p, j))) E_("avail")(m) + ((M_0(m) + w_(p, j)) g Delta h_(j, "climb")) / (eta_("climb") dot 3.6 times 10^6)
$
架次 $p$ 的全过程累计消耗总能量为各航段之和：
$
E_p = sum_(j=1)^(l+1) Delta E_j
$
返航安全余量硬约束要求：
$
E_p <= (1 - eta) E_("avail")(m) = 0.80 E_("avail")(m)
$

=== 实体机周转与两阶段电池充电时钟推进模型
1. *实体无人机时间轴推进约束*：
   若同一实体无人机 $u$ 先后执飞架次 $p$ 与架次 $p'$，则由于机体检查与新货箱装载需要法定周转时间，必须满足：
   $
   t_("start")^(p') >= t_("arr")^p + T_("prep")(m) + sum_(k in cal(K)_(p')) T_("load_box")
   $
2. *两阶段动力电池充电状态机*：
   动力电池作为独立流转资产，在架次 $p$ 消耗电能 $E_p$ 后，返航剩余电量为 $"SOC" = 1 - E_p / E_("avail")(m)$。
   根据锂电池两阶段物理充电特性，充饱所需时间 $Delta t_("charge")$ 建模如下：
   $
   Delta t_("charge") = cases(
     ((0.90 - "SOC") / 0.90 times 0.65 + 0.35) T_("full")(m)\, & "SOC" < 0.90,
     ((1.00 - "SOC") / 0.10 times 0.35) T_("full")(m)\, & "SOC" >= 0.90
   )
   $
   若该电池 $b$ 再次被指派给后续架次 $p''$，则该架次的起飞准备时刻必须满足充电机就绪时钟约束：
   $
   t_("start")^(p'') >= t_("arr")^p + Delta t_("charge")(b)
   $
3. *物资交付硬时限约束*：
   医疗物资与所有带有时限的首批物资，必须在预定截止时间前交付完成：
   $
   t_("deliv")^k <= T_("deadline")^k, quad forall k in cal(K)_("first")
   $

== 求解算法设计：基于状态机的大邻域自适应搜索（ALNS）

针对异构多机多车场带充电状态机的 VRPTW 难题，设计自适应大邻域搜索算法（ALNS），其求解流程如图 6-1 所示。

#figure(
  image("../../figures/fig_flow_q2.pdf", width: 75%),
  caption: [考虑实体机周转与两阶段充电状态推进的 ALNS 调度算法流程图],
) <fig_flow_q2>

核心算子包括：
- *优先级时序贪婪构造*：按物资紧急度与空间紧凑度构建初始可行路径；
- *Shaw 空间关联破坏算子*：计算服务区综合关联度 $R(i, j) = c_1 d_(i j) + c_2 |t_i - t_j|$，成片移除相近航段以释放重构空间；
- *多重遗憾值插入修复算子（Regret-2 / Regret-3）*：优先插入“不在当前最佳位置插入将产生巨大惩罚”的紧迫货箱；
- *时空冲突闭环修复机制*：沿时间轴推进机队与电池可用时钟，一旦检测到充电未就绪或无人机冲突，自动向后推移起飞时间窗口。

== 求解结果与调度方案综合讨论

=== 调度性能与核心指标
经过负荷均衡、满载压缩与动态换电流水线（ASAP/EDF）的深度优化，异构机队时空调度方案核心指标汇总于表 6-1。

#align(center)[
  #table(
    columns: (4.5cm, 3.5cm, 6.0cm),
    align: (left + horizon, center + horizon, left + horizon),
    stroke: none,
    table.hline(stroke: 1.2pt),
    table.header([*优化评估维度*], [*指标数值*], [*管理实践与决策意义*]),
    table.hline(stroke: 0.6pt),
    [全任务完工时间 (Makespan)], [*7353.1 秒 (2.04h)*], [由3.46h骤降至2.04h，**耗时缩短41.0% (提速1.42小时)**],
    [首批物资违约箱数 / 违约率], [*0 箱 / 0.0%*], [**100% 严格履约**，30箱急救与首批保障物资零延误送达],
    [期望时限违约箱数], [*8 箱 (原18箱)*], [大幅收窄10箱（减少55.6%），仅非紧迫剩余饮用水稍有平移],
    [总出动运输架次], [*28 架次*], [A型13架次、B型8架次、C型7架次，负载高度均衡],
    [全过程运输总电耗], [*79.915 kWh*], [由84.152 kWh降至79.915 kWh，**节能5.03% (省电4.24kWh)**],
    [多点串联协同回路], [*T002(S002-S004) & S010-S013*], [首批及东南部两组科学串联，累计节约航程近15km],
    [机队负荷均衡利用率], [*8架机完工于1.48~2.04h*], [彻底消除原方案轻型机大半时间闲置与重载机过劳瓶颈],
    table.hline(stroke: 1.2pt)
  )
]

=== 实体机与共享电池双甘特图验证
图 6-2 展示了 8 架实体无人机与 14 组共享电池在全生命周期内的时空协同甘特图。

#figure(
  image("../../figures/fig3_flight_and_charging_gantt.pdf", width: 95%),
  caption: [8 架实体运输机与 14 组共享电池两阶段充电状态协同演进甘特图],
) <fig_gantt>

从甘特图可以清晰观察到深度优化带来的卓越提升：
1. *机队全并发饱和作业与零冲突*：4 架 A 型机（U01~U04）在第一波完成各自点位的首批紧急物资配送后，换上基地预备电池立即无缝投入后续批次的高频周转，彻底改变了原方案在 1.5 小时后全部闲置的严重浪费局面。2 架 C 型重载机（U07、U08）分别执飞 3 次与 4 次，专注于 S001 大体量满载攻坚与 S003 高海拔重载，8 架无人机完工时刻紧密收敛在 1.48h 至 2.04h 之间；
2. *电池两阶段等效充电无缝流转*：深色飞行放电条结束后，电池立即进入恒流快速充电（SOC 回升至 90%）与恒压涓流阶段。由于备用电池组配置充裕（A型6组、B型4组、C型4组），返航无人机通过“即落即换”模式立即换装满电待命电池再起飞，实现了真正意义上的事件驱动滚动流水线；
3. *双多点回路的工程协同价值*：
   - 回路一（T002）：C 型机串联配送 S002 与 S004 首批物资，省去 1 次高达 272m 的重复爬升与 12.93km 航程；
   - 回路二（T024）：A 型机串联配送东南部相邻的 S010 与 S013 剩余食品，将原本孤立的轻载直飞整合，节约 1 个出动架次。
   两个多点回路均严格约束在各自片区内，完全兼容第四阶段的空间分区要求。